'use client';\n\nimport React, { useState, useEffect } from 'react';\nimport { \n  FileText, \n  Download, \n  User, \n  Calendar, \n  Tag, \n  Globe, \n  DollarSign,\n  Shield, \n  Database,\n  AlertCircle,\n  CheckCircle,\n  Clock,\n  Eye\n} from 'lucide-react';\n\ninterface SubmissionData {\n  id: string;\n  title: string;\n  abstract: string;\n  content: string;\n  keywords: string[];\n  authors: string[];\n  corresponding_author: string;\n  manuscript_type: string;\n  funding_information: string;\n  conflict_of_interest: string;\n  ethics_approval: string;\n  data_availability: string;\n  status: string;\n  submission_date: string;\n  created_at: string;\n  updated_at: string;\n  word_count: number;\n  manuscript_file_url: string;\n  author: {\n    id: string;\n    name: string;\n    email: string;\n  };\n  reviewer_assignments: any[];\n  reviews: any[];\n  user_role: {\n    is_author: boolean;\n    is_reviewer: boolean;\n    is_admin_or_editor: boolean;\n  };\n}\n\ninterface SubmissionViewerProps {\n  submissionId: string;\n  className?: string;\n}\n\nexport const SubmissionViewer: React.FC<SubmissionViewerProps> = ({\n  submissionId,\n  className = ''\n}) => {\n  const [submission, setSubmission] = useState<SubmissionData | null>(null);\n  const [loading, setLoading] = useState(true);\n  const [error, setError] = useState<string | null>(null);\n  const [activeTab, setActiveTab] = useState<'manuscript' | 'metadata' | 'reviews'>('manuscript');\n\n  useEffect(() => {\n    if (submissionId) {\n      loadSubmission();\n    }\n  }, [submissionId]);\n\n  const loadSubmission = async () => {\n    try {\n      setLoading(true);\n      setError(null);\n\n      const response = await fetch(`/api/submissions/${submissionId}`);\n      \n      if (!response.ok) {\n        const errorData = await response.json();\n        throw new Error(errorData.error || 'Failed to load submission');\n      }\n\n      const data = await response.json();\n      setSubmission(data);\n\n    } catch (err) {\n      setError(err instanceof Error ? err.message : 'Failed to load submission');\n    } finally {\n      setLoading(false);\n    }\n  };\n\n  const getStatusColor = (status: string) => {\n    switch (status) {\n      case 'submitted':\n        return 'bg-blue-100 text-blue-800';\n      case 'under_review':\n      case 'assigned_for_review':\n        return 'bg-yellow-100 text-yellow-800';\n      case 'revision_requested':\n        return 'bg-orange-100 text-orange-800';\n      case 'accepted':\n        return 'bg-green-100 text-green-800';\n      case 'rejected':\n        return 'bg-red-100 text-red-800';\n      case 'published':\n        return 'bg-purple-100 text-purple-800';\n      default:\n        return 'bg-gray-100 text-gray-800';\n    }\n  };\n\n  const formatDate = (dateString: string) => {\n    return new Date(dateString).toLocaleDateString('en-US', {\n      year: 'numeric',\n      month: 'long',\n      day: 'numeric',\n      hour: '2-digit',\n      minute: '2-digit'\n    });\n  };\n\n  const handleDownload = async () => {\n    if (!submission?.manuscript_file_url) {\n      alert('No manuscript file available for download');\n      return;\n    }\n\n    try {\n      // Create a temporary link to download the file\n      const link = document.createElement('a');\n      link.href = submission.manuscript_file_url;\n      link.download = `${submission.title.replace(/[^a-z0-9]/gi, '_').toLowerCase()}.pdf`;\n      link.target = '_blank';\n      document.body.appendChild(link);\n      link.click();\n      document.body.removeChild(link);\n    } catch (err) {\n      console.error('Download failed:', err);\n      alert('Failed to download manuscript file');\n    }\n  };\n\n  if (loading) {\n    return (\n      <div className={`bg-white rounded-lg shadow-sm border border-gray-200 ${className}`}>\n        <div className=\"p-8 text-center\">\n          <div className=\"animate-spin rounded-full h-8 w-8 border-b-2 border-blue-600 mx-auto mb-4\"></div>\n          <p className=\"text-gray-600\">Loading submission...</p>\n        </div>\n      </div>\n    );\n  }\n\n  if (error) {\n    return (\n      <div className={`bg-white rounded-lg shadow-sm border border-gray-200 ${className}`}>\n        <div className=\"p-8 text-center\">\n          <AlertCircle className=\"w-12 h-12 text-red-500 mx-auto mb-4\" />\n          <h3 className=\"text-lg font-medium text-gray-900 mb-2\">Error Loading Submission</h3>\n          <p className=\"text-red-600 mb-4\">{error}</p>\n          <button\n            onClick={loadSubmission}\n            className=\"px-4 py-2 bg-blue-600 text-white rounded-md hover:bg-blue-700 focus:outline-none focus:ring-2 focus:ring-blue-500\"\n          >\n            Try Again\n          </button>\n        </div>\n      </div>\n    );\n  }\n\n  if (!submission) {\n    return (\n      <div className={`bg-white rounded-lg shadow-sm border border-gray-200 ${className}`}>\n        <div className=\"p-8 text-center\">\n          <FileText className=\"w-12 h-12 text-gray-400 mx-auto mb-4\" />\n          <h3 className=\"text-lg font-medium text-gray-900 mb-2\">Submission Not Found</h3>\n          <p className=\"text-gray-600\">The requested submission could not be found.</p>\n        </div>\n      </div>\n    );\n  }\n\n  return (\n    <div className={`bg-white rounded-lg shadow-sm border border-gray-200 ${className}`}>\n      {/* Header */}\n      <div className=\"p-6 border-b border-gray-200\">\n        <div className=\"flex items-start justify-between\">\n          <div className=\"flex-1 min-w-0\">\n            <h1 className=\"text-2xl font-bold text-gray-900 mb-2\">\n              {submission.title}\n            </h1>\n            <div className=\"flex items-center space-x-4 text-sm text-gray-600 mb-4\">\n              <div className=\"flex items-center\">\n                <User className=\"w-4 h-4 mr-1\" />\n                <span>{submission.author.name}</span>\n              </div>\n              <div className=\"flex items-center\">\n                <Calendar className=\"w-4 h-4 mr-1\" />\n                <span>Submitted {formatDate(submission.submission_date)}</span>\n              </div>\n              <div className=\"flex items-center\">\n                <Eye className=\"w-4 h-4 mr-1\" />\n                <span>{submission.word_count.toLocaleString()} words</span>\n              </div>\n            </div>\n            <div className=\"flex items-center space-x-3\">\n              <span className={`inline-flex px-3 py-1 text-sm font-semibold rounded-full ${getStatusColor(submission.status)}`}>\n                {submission.status.replace('_', ' ').toUpperCase()}\n              </span>\n              <span className=\"text-sm text-gray-500 capitalize\">\n                {submission.manuscript_type.replace('_', ' ')}\n              </span>\n            </div>\n          </div>\n          \n          <div className=\"flex items-center space-x-3\">\n            {submission.manuscript_file_url && (\n              <button\n                onClick={handleDownload}\n                className=\"inline-flex items-center px-4 py-2 border border-gray-300 shadow-sm text-sm font-medium rounded-md text-gray-700 bg-white hover:bg-gray-50 focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-blue-500\"\n              >\n                <Download className=\"w-4 h-4 mr-2\" />\n                Download PDF\n              </button>\n            )}\n          </div>\n        </div>\n      </div>\n\n      {/* Navigation Tabs */}\n      <div className=\"border-b border-gray-200\">\n        <nav className=\"flex space-x-8 px-6\">\n          {[\n            { key: 'manuscript', label: 'Manuscript', icon: FileText },\n            { key: 'metadata', label: 'Metadata', icon: Tag },\n            { key: 'reviews', label: 'Reviews', icon: CheckCircle }\n          ].map(tab => {\n            const Icon = tab.icon;\n            return (\n              <button\n                key={tab.key}\n                onClick={() => setActiveTab(tab.key as any)}\n                className={`flex items-center py-4 px-1 border-b-2 font-medium text-sm ${\n                  activeTab === tab.key\n                    ? 'border-blue-500 text-blue-600'\n                    : 'border-transparent text-gray-500 hover:text-gray-700 hover:border-gray-300'\n                }`}\n              >\n                <Icon className=\"w-4 h-4 mr-2\" />\n                {tab.label}\n                {tab.key === 'reviews' && submission.reviews.length > 0 && (\n                  <span className=\"ml-2 bg-blue-100 text-blue-600 text-xs px-2 py-1 rounded-full\">\n                    {submission.reviews.length}\n                  </span>\n                )}\n              </button>\n            );\n          })}\n        </nav>\n      </div>\n\n      {/* Content */}\n      <div className=\"p-6\">\n        {activeTab === 'manuscript' && (\n          <div className=\"space-y-6\">\n            {/* Abstract */}\n            <div>\n              <h3 className=\"text-lg font-medium text-gray-900 mb-3\">Abstract</h3>\n              <div className=\"prose max-w-none\">\n                <p className=\"text-gray-700 leading-relaxed\">\n                  {submission.abstract || 'No abstract provided.'}\n                </p>\n              </div>\n            </div>\n\n            {/* Keywords */}\n            {submission.keywords && submission.keywords.length > 0 && (\n              <div>\n                <h3 className=\"text-lg font-medium text-gray-900 mb-3\">Keywords</h3>\n                <div className=\"flex flex-wrap gap-2\">\n                  {submission.keywords.map((keyword, index) => (\n                    <span\n                      key={index}\n                      className=\"inline-flex items-center px-3 py-1 rounded-full text-sm bg-blue-100 text-blue-800\"\n                    >\n                      {keyword}\n                    </span>\n                  ))}\n                </div>\n              </div>\n            )}\n\n            {/* Authors */}\n            {submission.authors && submission.authors.length > 0 && (\n              <div>\n                <h3 className=\"text-lg font-medium text-gray-900 mb-3\">Authors</h3>\n                <div className=\"space-y-2\">\n                  {submission.authors.map((author, index) => (\n                    <div key={index} className=\"flex items-center text-gray-700\">\n                      <User className=\"w-4 h-4 mr-2 text-gray-400\" />\n                      <span>{author}</span>\n                      {index === 0 && (\n                        <span className=\"ml-2 text-xs text-blue-600 bg-blue-100 px-2 py-1 rounded\">\n                          Corresponding Author\n                        </span>\n                      )}\n                    </div>\n                  ))}\n                </div>\n              </div>\n            )}\n\n            {/* Main Content */}\n            {submission.content && (\n              <div>\n                <h3 className=\"text-lg font-medium text-gray-900 mb-3\">Manuscript Content</h3>\n                <div className=\"prose max-w-none bg-gray-50 p-6 rounded-lg\">\n                  <div \n                    className=\"text-gray-700 leading-relaxed whitespace-pre-wrap\"\n                    dangerouslySetInnerHTML={{ __html: submission.content }}\n                  />\n                </div>\n              </div>\n            )}\n\n            {/* File Preview */}\n            {submission.manuscript_file_url && (\n              <div>\n                <h3 className=\"text-lg font-medium text-gray-900 mb-3\">Manuscript File</h3>\n                <div className=\"border border-gray-200 rounded-lg p-4\">\n                  <div className=\"flex items-center justify-between\">\n                    <div className=\"flex items-center\">\n                      <FileText className=\"w-8 h-8 text-red-500 mr-3\" />\n                      <div>\n                        <p className=\"font-medium text-gray-900\">Manuscript PDF</p>\n                        <p className=\"text-sm text-gray-500\">Click to download or view</p>\n                      </div>\n                    </div>\n                    <button\n                      onClick={handleDownload}\n                      className=\"px-4 py-2 bg-blue-600 text-white rounded-md hover:bg-blue-700 focus:outline-none focus:ring-2 focus:ring-blue-500\"\n                    >\n                      <Download className=\"w-4 h-4\" />\n                    </button>\n                  </div>\n                </div>\n              </div>\n            )}\n          </div>\n        )}\n\n        {activeTab === 'metadata' && (\n          <div className=\"space-y-6\">\n            <div className=\"grid grid-cols-1 md:grid-cols-2 gap-6\">\n              {/* Funding Information */}\n              <div>\n                <h3 className=\"flex items-center text-lg font-medium text-gray-900 mb-3\">\n                  <DollarSign className=\"w-5 h-5 mr-2\" />\n                  Funding Information\n                </h3>\n                <p className=\"text-gray-700\">\n                  {submission.funding_information || 'No funding information provided.'}\n                </p>\n              </div>\n\n              {/* Conflict of Interest */}\n              <div>\n                <h3 className=\"flex items-center text-lg font-medium text-gray-900 mb-3\">\n                  <Shield className=\"w-5 h-5 mr-2\" />\n                  Conflict of Interest\n                </h3>\n                <p className=\"text-gray-700\">\n                  {submission.conflict_of_interest || 'No conflicts of interest declared.'}\n                </p>\n              </div>\n\n              {/* Ethics Approval */}\n              <div>\n                <h3 className=\"flex items-center text-lg font-medium text-gray-900 mb-3\">\n                  <CheckCircle className=\"w-5 h-5 mr-2\" />\n                  Ethics Approval\n                </h3>\n                <p className=\"text-gray-700\">\n                  {submission.ethics_approval || 'No ethics approval information provided.'}\n                </p>\n              </div>\n\n              {/* Data Availability */}\n              <div>\n                <h3 className=\"flex items-center text-lg font-medium text-gray-900 mb-3\">\n                  <Database className=\"w-5 h-5 mr-2\" />\n                  Data Availability\n                </h3>\n                <p className=\"text-gray-700\">\n                  {submission.data_availability || 'No data availability statement provided.'}\n                </p>\n              </div>\n            </div>\n          </div>\n        )}\n\n        {activeTab === 'reviews' && (\n          <div className=\"space-y-6\">\n            {/* Review Status */}\n            <div className=\"bg-gray-50 p-4 rounded-lg\">\n              <h3 className=\"text-lg font-medium text-gray-900 mb-3\">Review Status</h3>\n              <div className=\"grid grid-cols-1 md:grid-cols-3 gap-4\">\n                <div className=\"text-center\">\n                  <div className=\"text-2xl font-bold text-blue-600\">\n                    {submission.reviewer_assignments.length}\n                  </div>\n                  <div className=\"text-sm text-gray-600\">Assigned Reviewers</div>\n                </div>\n                <div className=\"text-center\">\n                  <div className=\"text-2xl font-bold text-green-600\">\n                    {submission.reviews.filter(r => r.status === 'completed').length}\n                  </div>\n                  <div className=\"text-sm text-gray-600\">Completed Reviews</div>\n                </div>\n                <div className=\"text-center\">\n                  <div className=\"text-2xl font-bold text-orange-600\">\n                    {submission.reviewer_assignments.length - submission.reviews.filter(r => r.status === 'completed').length}\n                  </div>\n                  <div className=\"text-sm text-gray-600\">Pending Reviews</div>\n                </div>\n              </div>\n            </div>\n\n            {/* Reviews List */}\n            {submission.reviews.length > 0 ? (\n              <div className=\"space-y-4\">\n                <h3 className=\"text-lg font-medium text-gray-900\">Completed Reviews</h3>\n                {submission.reviews.map((review, index) => (\n                  <div key={review.id} className=\"border border-gray-200 rounded-lg p-4\">\n                    <div className=\"flex items-center justify-between mb-3\">\n                      <h4 className=\"font-medium text-gray-900\">Review #{index + 1}</h4>\n                      <div className=\"flex items-center space-x-2\">\n                        <span className={`px-2 py-1 text-xs font-semibold rounded-full ${\n                          review.recommendation === 'accept' ? 'bg-green-100 text-green-800' :\n                          review.recommendation === 'minor_revisions' ? 'bg-yellow-100 text-yellow-800' :\n                          review.recommendation === 'major_revisions' ? 'bg-orange-100 text-orange-800' :\n                          'bg-red-100 text-red-800'\n                        }`}>\n                          {review.recommendation.replace('_', ' ').toUpperCase()}\n                        </span>\n                        <span className=\"text-sm text-gray-500\">\n                          {formatDate(review.submitted_at)}\n                        </span>\n                      </div>\n                    </div>\n                    {/* Review content would be shown here if accessible to current user */}\n                    {(submission.user_role.is_author || submission.user_role.is_admin_or_editor) && (\n                      <div className=\"text-gray-700\">\n                        <p className=\"text-sm text-gray-500 mb-2\">Review comments are available to authors and editors.</p>\n                      </div>\n                    )}\n                  </div>\n                ))}\n              </div>\n            ) : (\n              <div className=\"text-center py-8\">\n                <Clock className=\"w-12 h-12 text-gray-400 mx-auto mb-4\" />\n                <h3 className=\"text-lg font-medium text-gray-900 mb-2\">No Reviews Yet</h3>\n                <p className=\"text-gray-600\">\n                  {submission.reviewer_assignments.length > 0\n                    ? 'Reviews are in progress. Check back later for updates.'\n                    : 'No reviewers have been assigned to this submission yet.'\n                  }\n                </p>\n              </div>\n            )}\n          </div>\n        )}\n      </div>\n    </div>\n  );\n};"